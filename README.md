```
#pip3 install --upgrade virtualenv
#python3 -m venv .venv;
#source .venv/bin/activate;
#which python3;

has to use "sudo su" + installs;
otherwise python cant find library (because of sudo)
and led does not work without sudo :/


sudo pip3 install slack_bolt rpi_ws281x python-dotenv colour;

sudo pip3 install slack_bolt;\
sudo pip3 install rpi_ws281x;\
sudo pip3 install python-dotenv;
sudo pip3 install colour;
#sudo pip3 install rpi_ws281x adafruit-circuitpython-neopixel;
#sudo pip3 install RPi.GPIO
#sudo apt-get install RPi.GPIO
#sudo python3 -m pip install --force-reinstall adafruit-blinka; (did not work)
#pip3 install python-decouple;
#pip3 install pycrosskit
```
